apiVersion: v1
kind: Service
metadata:
  name: attendanceapp-service 
spec:
  type: LoadBalancer
  selector:
    app: attendance-app
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP